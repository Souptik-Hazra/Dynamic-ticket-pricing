import * as userService from './service/user.service.js';

/**
 * 📦 Users Module Public API
 * 
 * Only services should be exported here. Models and Repositories are internal.
 */
export { userService };
