import * as broadcaster from '../../shared/utils/broadcaster.js';

/**
 * 📦 Notifications Module Public API
 * 
 * Note: Broadcaster is shared, but we could wrap it here if needed.
 */
export { broadcaster };
