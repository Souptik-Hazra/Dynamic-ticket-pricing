/**
 * 🖼️ Media Utility (Dummy for Stability)
 */
export const processImage = async (buffer) => buffer;
export const deleteFile = async (path) => true;
export const generateBrandedQR = async (data) => 'dummy-qr-data';

export default { processImage, deleteFile, generateBrandedQR };
