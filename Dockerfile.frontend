# Frontend Dockerfile (Vite/React)
FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy source
COPY . .

# Build for production (optional, here we run dev for flexibility)
EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host"]
